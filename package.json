{
  "name": "aiwp-support-chat-worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "cloudflare": {
    "bindings": {
      "SUPPORT_ROOM": {
        "description": "Durable Object namespace for WooPilot live support rooms. One object coordinates websocket clients for each site/room."
      },
      "AIWP_SIGNING_SECRET": {
        "description": "Set this to the signing secret generated by the WooPilot WordPress plugin. It verifies short-lived room tokens."
      }
    }
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260601.0",
    "typescript": "^6.0.2",
    "wrangler": "^4.0.0"
  }
}
